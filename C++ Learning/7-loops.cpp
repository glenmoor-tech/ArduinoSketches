#include <iostream>
#include <array>
using namespace std;

int main() {
    while (true) { // Since condition will always be true
                   // this loop will go on forever
        cout << "bacon!" << "\n";
    } // CTRL + C to exit
}